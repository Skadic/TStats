import { Tournament } from "../lib/api/v1types";
import { Component } from "../lib/types";

const TournamentView: Component<{tournament: Tournament}> = (props) => {
  return <></>
} 

export default TournamentView
